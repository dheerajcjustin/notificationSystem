{
      "name": "y",
      "version": "1.0.0",
      "description": "task by enterprisebot ",
      "main": "index.js",
      "scripts": {
            "start": "concurrently \"npm run backend\" \"npm run frontend\"",
            "backend": "cd backend && npm run dev",
            "frontend": "cd frontend && npm run dev",
            "setup": "concurrently \"npm run setup-forntend\" \"npm run setup-backend\"",
            "setup-forntend": "cd frontend && npm i",
            "setup-backend": "cd backend && npm i",
            "test": "echo \"Error: no test specified\" && exit 1"
      },
      "author": "dheeraj c justine",
      "license": "ISC",
      "devDependencies": {
            "concurrently": "^8.2.2"
      }
}
